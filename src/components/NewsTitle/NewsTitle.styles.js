import styled from "styled-components";

export const Wrapper = styled.div`
  width: min(100%, 80rem);
  border-top: 2px solid var(--grey);
  padding-block: 8rem;

  h2 {
    font-size: 6.4rem;
    font-weight: 800;
  }

  h3 {
    font-size: 3.6rem;
    font-weight: 400;
  }
`;
